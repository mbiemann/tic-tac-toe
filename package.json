{
  "name": "tictactoe",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel --https",
    "build": "rm -rf .parcel-cache dist && parcel build --no-scope-hoist --no-content-hash --no-source-maps --public-url .",
    "refresh": "rm -rf .parcel-cache dist",
    "clean": "rm -rf .parcel-cache dist node_modules package-lock.json"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "parcel": "^2.9.3",
    "postcss": "^8.4.30",
    "process": "^0.11.10",
    "tailwindcss": "^3.3.3"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
